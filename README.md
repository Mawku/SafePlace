# SafePlace
safe place is an application to try and stop shoplifter before they leave the shop by detecting their behavior via camera
this is a demo that run entirely off the browser and can use the camera of the device being used, but the real deal should run off a server and connect to cctv's
